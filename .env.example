# ── Nestview environment variables ───────────────────────────────────────────
# Copy this file to .env and edit as needed.
# docker compose automatically loads .env from the project root.
# Log retention and exited container TTL are configured in the Settings UI.

# Port Nestview is exposed on (default: 8080)
NESTVIEW_PORT=8080

# Optional shared secret between backend and collector.
# Set the same value in both services to reject unauthenticated collector POSTs.
NESTVIEW_COLLECTOR_KEY=

# Optional API key that gates all user-facing write endpoints (container actions,
# settings changes). When set, the frontend prompts for this key on load and
# sends it as an X-API-Key header. Leave empty for trusted home-network deployments.
# IMPORTANT: If you expose port 8080 publicly, set this to a strong random value.
NESTVIEW_API_KEY=

# How often the collector polls Docker for container stats, in seconds (default: 10)
POLL_INTERVAL=10

# How often the collector flushes buffered logs to the backend, in seconds (default: 5)
LOG_BATCH_INTERVAL=5
